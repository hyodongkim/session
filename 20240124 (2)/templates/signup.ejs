<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>회원가입화면</title>
    </head>
    <body>
        <form method="post" action="/signup">
            <input type="text" name="id" value="<%=id%>" required maxlength="10" minlength="5">
            <input type="password" name="pw" value="<%=pw%>" required maxlength="20" minlength="5">
            <button type="submit">회원가입</button>
        </form>
    </body>
</html>